#!/usr/bin/env node
import { WARDEN_VERSION } from "./index.js";

console.log(`mcp-warden v${WARDEN_VERSION}`);
console.log("Warden is under active development. Nothing is proxied yet — see the roadmap:");
console.log("https://github.com/parveshsaini/warden#roadmap");
