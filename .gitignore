node_modules/
dist/
.terraform/
coverage/
*.log
.env
.env.*
.DS_Store
# local dev config at the repo root only (examples/ stays tracked)
/warden.config.yaml
/warden.config.yml
/warden.config.json

# internal working notes — never publish
docs/build-log.md
warden-audit.jsonl
demo/
